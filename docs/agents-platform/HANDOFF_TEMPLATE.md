# GPTBot Agents — Handoff (ШАБЛОН: скопируй в HANDOFF.md и заполни ВСЕ разделы фактами)

## 1. Состояние
- Дата:
- Ветка:
- HEAD:
- Завершённый этап:
- Следующий этап:
- Рабочее дерево: (clean / что осталось и почему)

## 2. Что сделано
Только фактически выполненное. Без планов и намерений.

## 3. Изменённые файлы
Для каждого: зачем изменён; что содержит; важные ограничения.

## 4. Архитектурные решения
Только НОВЫЕ решения этого этапа (и продублируй их в DECISIONS.md с номером).

## 5. Что сознательно не сделано
Чтобы следующий агент не принял это за забытое.

## 6. Проверки
Точная команда → точный результат (exit-код, числа pass/fail):
- build; - test; - typecheck; - lint (свои файлы); - targeted tests.

## 7. Известные проблемы
Разделить: существовали до этапа / появились в этапе / внешние блокеры.

## 8. Следующая задача
Одно конкретное задание следующему агенту (= STATE.json.next_stage + суть).

## 9. Acceptance criteria следующего этапа
Измеримые критерии (числа, команды, поведение).

## 10. Команды для старта
Что следующий агент выполняет первыми (git status/log, чтение STATE/HANDOFF, baseline-подмножество).

## 11. Риски
Что нельзя случайно сломать в этом этапе.

## 12. Rollback
Как отменить ТОЛЬКО этот этап (обычно `git revert <SHA>`; отметь несовместимые с revert вещи — ALTER-колонки и т.п.).
