# Исполнение роадмапа: 6 сентября 2026

## Выполнено в кандидате

В актуальный checkout перенесён и доработан контур consumer billing: Telegram OIDC, непрозрачный аккаунт, Click/Payme, атомарный журнал, календарные периоды, квоты до запроса к модели, ограниченные переключения и бюджет попыток. Старый общий payment webhook закрыт с 410; ручное обещание активации за USD удалено. Реальные оплаты не включены.

Миграция `0064_gpt_consumer_billing.sql` сохраняет номера 0062/0063 для AEO. Добавлена запись версии/URL/языка принятых условий. Checkout отвергает отсутствующие и устаревшие условия; ссылки условий разрешены только на HTTPS gptbot.uz. Payme TTL после Prepare привязан к времени транзакции провайдера. Тестовые платежи не попадают в старое зеркало live-подписок.

Аккаунты получают раздельную историю и архивы в браузере. Выход отзывает сессию и очищает transcript cookies. Чужой аккаунт/недоступный account API не раскрывает старый разговор. Незавершённый платёж можно продолжить у того же провайдера; фоновая проверка ограничена шестью попытками, ручная проверка остаётся доступной. Смена вкладки обновляет состояние. Нет выдуманного события покупки при чтении кабинета.

Пробельный stream допускает резервную модель до начала содержательного ответа. Ошибочный, оборванный или остановленный stream освобождает пакет успешных ответов. Бюджет фактических попыток платных моделей не возвращается: провайдер может тарифицировать попытку. При сбое БД запрос не идёт в платную модель без учёта. Бесплатная цепочка допускает только :free и price cap 0.

Убраны неподтверждённые FAQ/SEO-гарантии из административных подсказок, уточнены статические llms-справочники. Защищённые поисковые заголовки/URL не редактировались. Hreflang без установленной опубликованной переводной пары оставлен для отдельного контентного решения.

## Проверки перед выпуском

- 597/597 тестов общего набора; 30 проверок нового backend входят в это число, их не надо суммировать повторно.
- TypeScript приложения и Functions: без ошибок. Scoped ESLint backend/frontend: без ошибок.
- Синтетический SQLite прогон: 1 000 запросов, 800 успешных и 200 освобождённых, внешних вызовов 0. Это не оценка пропускной способности production D1.
- В браузере на локальном стенде 360×800: test account → test invoice pending → test callback → пакет 300 → ответ → остаток 299; доступ и история исчезают после выхода. Проверены RU/UZ тексты и отдельное продолжение pending-счёта. Реального входа в Telegram и списания денег не было.
- Актуальный каталог OpenRouter сохранён в model-catalogue.json: выбранные модели присутствуют, объявленные цены укладываются в пределы. Каталог не доказывает наличие свободной мощности.
- Production D1 экспортирован до миграции: 27 282 337 байт, SHA-256 и путь в d1-backup.json. Содержимое резервной копии вне репозитория, не публикуется. На момент экспорта новых billing-таблиц в production не было.

Эти проверки описывали первый кандидат. Ниже — окончательные результаты опубликованной версии.

## Финальный опубликованный результат

Runtime `644a94a3cd0da5f1f7ef487d5649ef72f8fe7ee5`, Pages deployment `6907261a-5483-4418-8f1b-9a4b075bbe8f`. Основная интеграция — `48e1d206`; следующий небольшой коммит уточняет текст о локальном списке чатов и обработке сообщений на сервере. Оба находятся в origin/main.

- **598/598** тестов окончательного общего набора; **39/39** отдельного набора release/административных/новых backend-проверок. Между наборами есть пересечения, суммарный «637» не используем. Приложение и Functions проходят TypeScript, scoped ESLint и secret scan чистые. Полная build:cf включает admin, prerender, sitemap и stamp: **914 файлов**.
- Guard подтвердил **10/10** неизменённых защищённых SEO-контрактов. На live все **288 страниц** совпадают с HTML-контрактами финальной сборки, набор sitemap URL сохранён, восемь непосредственно подключённых CSS/JS проверены по SHA-256. Admin и auth config доступны. Артефакт: `a801903c1c78c5e610097cb2670cbbdd1755b490c20a4672374dc786025f14cf`.
- Миграция 0064 применена и зарегистрирована в production; проверены **14 новых таблиц**, перед выпуском аккаунтов/счетов/периодов было 0. Связи Pages и существующие секреты сохранены. Единственная осознанная смена обычной переменной в основном выпуске — последняя платная fallback-модель на бесплатный аварийный вариант.
- На настоящем сайте в браузере при 393×851 новый запрос на узбекском **7 + 5 → 12**, модель minimax/minimax-m3, ответ завершён. Старый ответ 2 + 2 в истории не считается новым тестом. D1 readback: 1 завершённый запрос, 0 зависших резерваций, 0 платных попыток, 0 аккаунтов, 0 счетов/периодов.
- RU/UZ кабинет явно помечен «Скоро / Tez orada», сообщает, что оплата не подключена; активных платёжных кнопок нет. Русский диалог не создаёт горизонтального переполнения: pageWidth=viewport=393. Account API: 200, no-store, noindex; mode=null, providers=[], loginAvailable=false. Неавторизованные внутренние операции запрещены; неподтверждённые Click/Payme callbacks возвращают протокольную ошибку, старый webhook — 410.
- Финальный `deployment.json` содержит предыдущий рабочий runtime/deployment; начальный receipt сохранён отдельно. `live-verification.json`, `account-live.json`, `disabled-endpoints-live.json` и `d1-live-readback.json` содержат проверки без приватных текстов посетителей и credentials.

## Обнаруженные ограничения, которые не скрываем

Восстановление старого production export в локальный SQLite обнаружило прежний FK mismatch `sotuvchi_categories → sotuvchi_stores` и CHECK в `lead_radar_signal_posts`. Для импорта копии прежние FK/CHECK временно отключались; перед новой миграцией CHECK включён обратно. Двукратное применение миграции сохранило количество записей всех **147** прежних таблиц и ровно тот же прежний CHECK-результат. Это проверка отсутствия изменений старых данных, **не заявление, что вся старая БД свободна от нарушений или уже восстановлена в production**. Исходный export не изменялся. Исправление старых данных требует отдельного проверенного плана, добавлено в P1.

Попытка получить mobile PageSpeed для RU/UZ чата вернула **HTTP 429 от API Google**. Score/CWV остаются неизвестными; отсутствие измерения не равно хорошей скорости. Синтетический нагрузочный тест не заменяет D1 capacity test. Новая подписка ещё не прошла настоящий OIDC login, merchant sandbox/live acceptance, фискализацию и реальный возврат. Maintenance hook подготовлен в существующем worker, но сам worker и его расписание в этой задаче отдельно не разворачивались.

## Внешние зависимости

Нужны Telegram Login Client ID/Secret и разрешённый callback; Click/Payme test/live реквизиты и merchant acceptance; утверждённые RU/UZ условия и реквизиты фискализации. Этих данных нет в текущей конфигурации production. Их нельзя выдумать или заменить зелёным unit-тестом. Подробности: CONFIGURATION-RU.md.

Uzum пока не имеет проверенного merchant-контракта и не изображается подключённым. Автосписание не реализуется вместо согласованного ручного продления. CWV/когортная выручка и независимый прямой AI-провайдер остаются следующими измеряемыми этапами, как указано в роадмапе.
